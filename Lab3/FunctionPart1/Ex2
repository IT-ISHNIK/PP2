"""Read in a Fahrenheit temperature. Calculate and display the equivalent centigrade temperature. The following formula is used for the conversion: C = (5 / 9) * (F -36 32)"""
def change(F):
    C = int((5 / 9) * (F - 32))
    print("Equivalente temperature for ", F," fahrenheits is ", C, "celcium")
F = int(input("Input value in fahrenheits: "))
change(F)
